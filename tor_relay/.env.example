# Tor Relay Environment Variables
# Copy this to .env and customize as needed

# Relay configuration
TOR_NICKNAME=operaTor
TOR_CONTACT_INFO=torrelay_operator.isothermally@8shield.net

# Optional: Override bandwidth limits (in KBytes)
# TOR_BANDWIDTH_RATE=1000
# TOR_BANDWIDTH_BURST=1500

# Optional: Ports (if you need to change from defaults)
# TOR_OR_PORT=8443
# TOR_DIR_PORT=8444

# Beszel Agent Configuration Example
# Copy this file to .env and replace the values with your actual credentials
#
# IMPORTANT: Two-Stage Setup Required
# ======================================
# The Beszel agent requires credentials that are generated AFTER the hub is deployed.
# On first deployment, the agent will fail to connect - this is expected!
#
# First-Time Setup Process:
# -------------------------
# 1. Deploy with ansible-playbook (agent will start but fail to authenticate)
# 2. Access Beszel hub at http://YOUR_SERVER_IP:8090
# 3. Create admin account (first-time login)
# 4. Click "Add System" button
# 5. System Name: voyager (or your preferred name)
# 6. Host/IP: /beszel_socket/beszel.sock (IMPORTANT: Use Unix socket path)
# 7. Copy the TOKEN and KEY values shown in the dialog
# 8. Update .env file with these credentials (see below)
# 9. Restart the agent: docker compose restart beszel-agent
#
# Optional: Add to Ansible Vault for future deployments
# -----------------------------------------------------
# ansible-vault edit group_vars/all/vault.yml
# Add:
#   vault_beszel_agent_token: "your-token-here"
#   vault_beszel_agent_key: "your-key-here"
# Then update deploy_docker_env role template to use these variables

BESZEL_AGENT_TOKEN=changeme
BESZEL_AGENT_KEY=changeme
